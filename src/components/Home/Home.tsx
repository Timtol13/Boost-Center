import React from 'react'
import './Home.modul.scss'
import { ScrollBar } from '../ScrollBar/ScrollBar'

export const Home = () => {
  return (
    <div className={'main'}>
        <ScrollBar />
    </div>
  )
}
